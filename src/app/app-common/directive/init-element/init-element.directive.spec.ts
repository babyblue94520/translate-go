import { InitElementDirective } from './init-element.directive';

describe('InitElementDirective', () => {
  it('should create an instance', () => {
    const directive = new InitElementDirective();
    expect(directive).toBeTruthy();
  });
});
